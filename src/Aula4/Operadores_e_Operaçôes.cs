namespace Aula4
{
     class Operadores_e_Operaçôes
    {
        static void Main(string[] args)
        {
            int num = 10;

            bool res = (5>3) | (10<3);   
                                // & = and / E
                                // | = Or / ou
                                // retorna verdadeiro ou falço 
                                // > maior que // < menor do que // != diferente de // >= maior igua // =< menor igual
            num++;              //num += 1;    // num = num + 1;
                                // + * / = 
            Console.WriteLine(res);
        }
    }
}